{
  "name": "@anointedcoder/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Pure helper utilities shared between web and api.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "@anointedcoder/eslint-config": "workspace:*",
    "@anointedcoder/tsconfig": "workspace:*",
    "eslint": "^8.57.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
